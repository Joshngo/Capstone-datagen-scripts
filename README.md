To run

Run the server, and then the client right after.

The CSV file should be created when the program finishes executing.

To change the duration of the runtime, there is a "runTime" variable in the main function. Change it (in seconds) to run however long it needs to.
